package edu.colostate.cs414.d.pizza.api.user;

public class Chef extends User {
    public Chef(String userName, String password, UserType userType) {
        super(userName, password, userType);
    }
}
